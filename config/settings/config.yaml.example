databases:
  develop:
    name: name
    host: host
    port: 5432
    user: user
    password: password
  test:
    name: name
    host: host
    port: 5432
    user: user
    password: password

mongodb:
  name: name
  host: host
  port: port
  user: user
  password: password

secret_key: super_secret_key

sendgrid_api_key: super_secret_key

email:
  default_from: mail@domain.coop
  bcc: mail@domain.coop

cors:
  whitelist:
    - https://host:port
  allowed:headers:
    - allowed_header